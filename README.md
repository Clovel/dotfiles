# Clovel's dotfiles

This repository contains my configuration dotfiles ofr Linux and/or macOS.

# Tips
## macOS
### `bash`
Install the latest `bash` executable from homebrew, and set the terminal to use this `bash` (most likely located at `/usr/local/bin/bash`) instead of the default `/bin/bash`.
